# Product entry mode plan

1. Add the account preference migration and `/v1/me` contract coverage.
2. Persist nullable catalog references when shopping items are created and test no catalog side effects.
3. Implement account-synced Settings and temporary candidates in Web with regression tests.
4. Implement the same profile setting and candidate flow in Android, extending the existing offline create payload.
5. Update contract/context/release metadata, run focused tests, full required validation, review, and deployment impact.
