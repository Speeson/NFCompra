{
  "name": "@nfcompra/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --no-warnings --test test/migrations.wrangler.node-test.mjs && vitest run",
    "admin:delete-user": "node --experimental-strip-types scripts/admin-delete-user.ts",
    "catalog:build-sql": "node --experimental-strip-types scripts/build-catalog-sql.ts",
    "catalog:build-mercadona": "node --experimental-strip-types scripts/build-mercadona-catalog.ts",
    "db:migrate:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^4.20250726.0",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.30.0"
  }
}
